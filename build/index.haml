!!!
%html
  %head
    %meta{charset: 'utf-8'}
    %title LOL League Schedule

    %meta{name: 'HandheldFriendly', content: 'true'}
    %meta{name: 'viewport', content: 'width=device-width, height=device-height, user-scalable=no'}

    :css
      #{include('icons.css')}
      #{include('filters.css')}
      #{include('style.css')}

    :javascript
      #{include('times.js')}
      #{include('filters.js')}
      #{include('links.js')}
      #{include('reveals.js')}

    <!-- Page generated #{generated}, data file generated #{data_generated} -->
  %body
    #page-wrapper
      #filters.clearfix
        != partial('filters', { source: source })

      - source.leagues.select { |league| league.stream_url }.each do |league|
        != partial('league_stream', { league: league })
      - source.matches.sort_by { |match| Time.parse(match.time) }.reverse_each do |match|
        != partial('match', { match: match })
      #no-results.match
        %div There are no matches that fit all the filters you have applied.
    #backdrop
