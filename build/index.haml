!!!
%html
  %head
    %meta{charset: 'utf-8'}
    %title= title

    %meta{name: 'HandheldFriendly', content: 'true'}
    %meta{name: 'viewport', content: 'width=device-width, height=device-height, user-scalable=no'}

    :css
      #{include('css/icons.css')}
      #{include('css/filters.css')}
      #{include('css/style.css')}
  
    :javascript
      var data = #{include('data.json')};

      #{include('js/iscroll-infinite.js')}
      #{include('js/list.js')}
      #{include('js/times.js')}
      #{include('js/js.js')}
      #{include('js/filters.js')}
      #{include('js/links.js')}
      #{include('js/reveals.js')}
  %body
    #page-wrapper
      != partial('filters', { leagues: leagues })

      %h1= title

      #matches-root
        #scroller
          #scroller-inner
      #no-results.match.filter-no-match
        .data There are no matches that fit all the filters you have applied.

      #links.clearfix
        - Build::Html::SEASONS.each_pair do |year, file|
          %a{href: "/#{file if file != 'index.html'}"} #{year} Schedule

      #footer<
        Page generated&nbsp;
        %span.footer-time{data: { value: generated }}
        , data file generated&nbsp;
        %span.footer-time{data: { value: data_generated }}
        \.
