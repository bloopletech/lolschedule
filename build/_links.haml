.links.bar.clearfix
  %h3 Schedules:
  - Build::Html::SEASONS.each_pair do |link_year, file|
    - if year == link_year
      %div #{link_year}
    - else
      %a{href: "/#{file if file != 'index.html'}"} #{link_year}