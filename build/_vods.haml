- multiple = vod_urls.length > 1
- vod_urls.each_with_index do |video, i|
  %a.data.vod{href: video['url'], data: { id: video['id'], start: video['start'] }, target: '_blank', class: "#{'revealed' if i == 0}"}
    - if multiple
      vod #{i + 1}
    - else
      vod
- if multiple
  %a.data.reveal{href: '#'} …