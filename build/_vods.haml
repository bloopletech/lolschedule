.vods
  - if game_urls.length == 1
    .games
      %a.revealed{href: game_urls.first, target: '_blank'} vod
  - else
    .games.with-reveal
      - game_urls.each_with_index do |url, i|
        %a{href: url, target: '_blank', class: "#{'revealed' if i == 0}"}
          %span vod #{i + 1}
    %a.reveal{href: '#'} …